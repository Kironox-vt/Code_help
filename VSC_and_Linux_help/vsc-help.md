# My guide to Visual Studio Code commands

To open code in VSC:

```
code -r (file name)
```

To create a file use:

```
echo "(first line of text)" > (file name).(what file it is)
```

For other things its linux I think