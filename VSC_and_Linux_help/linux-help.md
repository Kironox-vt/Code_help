# My guide to linux commands

To create folders:

```
mkdir (folder name)
```

To go inside directories:

```
cd (directory)
```

To create files:

```
touch (file name)
```
or
```
echo "(first line)" > (file name)
```